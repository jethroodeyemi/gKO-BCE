# ============================================================================
#  KO-BCE / gKO-BCE dependencies
# ============================================================================
#
# --- PyTorch (install according to your CUDA runtime) ---
# For CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# --- ESM-IF1 (inverse folding) extras ---
# The ESM-IF1 structural encoder needs PyTorch Geometric + scatter/sparse.
# Install matching your torch/CUDA build, e.g. for torch 2.1.2 + cu121:
#   pip install torch-scatter -f https://data.pyg.org/whl/torch-2.1.2+cu121.html
#   pip install torch-geometric

# --- Core structural biology ---
biopython>=1.80
biotite>=0.40.0

# --- Protein language models (ESM-2 / ESM-1v / ESM-IF1) ---
fair-esm>=2.0.0

# --- Scientific / ML stack ---
pandas>=1.4.0
numpy>=1.22.0
scikit-learn>=1.0.0
scipy>=1.7.0

# --- Utilities ---
requests>=2.28.0
tqdm>=4.64.0
matplotlib>=3.5.0

# Note: CD-HIT (needed only for regenerating homology-aware splits during training)
# is installed separately:
#   conda install -c bioconda cd-hit
# Optional, for uploading released weights to the Hugging Face Hub:
#   pip install huggingface_hub
